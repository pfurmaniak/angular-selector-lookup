# Angular selector lookup

This simple extension finds all *.ts files that define a specific Angular selector

## Usage

Launch *Quick Open*:
- Linux `Ctrl + Shift + P`
- macOS `⌘ + Shift + P`
- Windows `Ctrl + Shift + P`

Run the following command:
```
Angular: Find selector
```